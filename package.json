{
  "name": "student-mgmt-system",
  "version": "1.0.0",
  "description": "Student Result & Attendance Management System",
  "scripts": {
    "install:all": "npm install --include=dev && cd backend && npm install --include=dev && cd ../frontend && npm install --include=dev",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "seed": "cd backend && npm run seed",
    "seed:demo": "cd backend && npm run seed:demo",
    "deploy:render": "npm run build && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
